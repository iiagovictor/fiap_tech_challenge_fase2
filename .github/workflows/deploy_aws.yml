name: '[PROD] Deploy Infra AWS'

on:
  push:
    branches:
      - main

jobs:
  setup:
    runs-on: ubuntu-latest
    outputs:
      python_version: ${{ steps.vars.outputs.python_version }}
      aws_region: ${{ steps.vars.outputs.aws_region }}
      aws_account_id: ${{ steps.vars.outputs.aws_account_id }}
      s3_bucket: ${{ steps.vars.outputs.s3_bucket }}
      s3_key: ${{ steps.vars.outputs.s3_key }}
      backend_region: ${{ steps.vars.outputs.backend_region }}
      tf_auto_approve: ${{ steps.vars.outputs.tf_auto_approve }}
      tf_destroy: ${{ steps.vars.outputs.tf_destroy }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install yq and read config.yaml variables
        id: vars
        run: |
          sudo wget https://github.com/mikefarah/yq/releases/download/v4.43.1/yq_linux_amd64 -O /usr/bin/yq
          sudo chmod +x /usr/bin/yq
          echo "python_version=$(yq '.setup.python_version // "3.11"' config.yaml)" >> $GITHUB_OUTPUT
          # Região principal
          echo "aws_region=$(yq '.aws.region' config.yaml)" >> $GITHUB_OUTPUT
          # Backend
          echo "aws_account_id=$(yq '.terraform.backend.account_id' config.yaml)" >> $GITHUB_OUTPUT
          echo "s3_bucket=$(yq '.terraform.backend.s3_bucket' config.yaml)" >> $GITHUB_OUTPUT
          echo "s3_key=$(yq '.terraform.backend.key' config.yaml)" >> $GITHUB_OUTPUT
          echo "backend_region=$(yq '.terraform.backend.region // .aws.region' config.yaml)" >> $GITHUB_OUTPUT
          # Flags de execução
          echo "tf_auto_approve=$(yq '.terraform.auto_approve' config.yaml)" >> $GITHUB_OUTPUT
          echo "tf_destroy=$(yq '.terraform.destroy' config.yaml)" >> $GITHUB_OUTPUT

      - name: Validate required variables
        run: |
          EXIT_CODE=0
          
          # Validação das variáveis de setup
          if [ -z "${{ steps.vars.outputs.python_version }}" ]; then
            echo "❌ Error: python_version is required"
            EXIT_CODE=1
          else
            echo "✅ python_version: ${{ steps.vars.outputs.python_version }}"
          fi
          
          # Validação das variáveis AWS
          if [ -z "${{ steps.vars.outputs.aws_region }}" ]; then
            echo "❌ Error: aws_region is required"
            EXIT_CODE=1
          else
            echo "✅ aws_region: ${{ steps.vars.outputs.aws_region }}"
          fi
          
          # Validação do backend Terraform
          if [ -z "${{ steps.vars.outputs.aws_account_id }}" ]; then
            echo "❌ Error: terraform.backend.account_id is required"
            EXIT_CODE=1
          else
            echo "✅ aws_account_id: ${{ steps.vars.outputs.aws_account_id }}"
          fi
          
          if [ -z "${{ steps.vars.outputs.s3_bucket }}" ]; then
            echo "❌ Error: terraform.backend.s3_bucket is required"
            EXIT_CODE=1
          else
            echo "✅ s3_bucket: ${{ steps.vars.outputs.s3_bucket }}"
          fi
          
          if [ -z "${{ steps.vars.outputs.s3_key }}" ]; then
            echo "❌ Error: terraform.backend.key is required"
            EXIT_CODE=1
          else
            echo "✅ s3_key: ${{ steps.vars.outputs.s3_key }}"
          fi
          
          if [ -z "${{ steps.vars.outputs.backend_region }}" ]; then
            echo "❌ Error: terraform.backend.region is required"
            EXIT_CODE=1
          else
            echo "✅ backend_region: ${{ steps.vars.outputs.backend_region }}"
          fi
          
          # Validação das flags de execução
          if [ -z "${{ steps.vars.outputs.tf_auto_approve }}" ]; then
            echo "⚠️  Warning: terraform.auto_approve not set, defaulting to false"
            echo "tf_auto_approve=false" >> $GITHUB_OUTPUT
          else
            echo "✅ tf_auto_approve: ${{ steps.vars.outputs.tf_auto_approve }}"
          fi
          
          if [ -z "${{ steps.vars.outputs.tf_destroy }}" ]; then
            echo "⚠️  Warning: terraform.destroy not set, defaulting to false"
            echo "tf_destroy=false" >> $GITHUB_OUTPUT
          else
            echo "✅ tf_destroy: ${{ steps.vars.outputs.tf_destroy }}"
          fi
          
          if [ $EXIT_CODE -ne 0 ]; then
            echo ""
            echo "❌ Validation failed! Please check your config.yaml file."
            exit 1
          fi
          
          echo ""
          echo "✅ All required variables validated successfully!"

  terraform_apply:
    needs: setup
    if: ${{ needs.setup.outputs.tf_destroy != 'true' }}
    runs-on: ubuntu-latest
    environment: prod
    steps:
      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-region: ${{ needs.setup.outputs.aws_region }}
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}

      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ needs.setup.outputs.python_version }}

      - name: Create S3 Backend Bucket if not exists
        run: |
          BUCKET_NAME="${{ needs.setup.outputs.s3_bucket }}"
          REGION="${{ needs.setup.outputs.backend_region }}"
          
          echo "🔍 Checking if bucket $BUCKET_NAME exists..."
          
          if aws s3 ls "s3://$BUCKET_NAME" 2>&1 | grep -q 'NoSuchBucket'; then
            echo "📦 Bucket does not exist. Creating..."
            
            if [ "$REGION" = "us-east-1" ]; then
              aws s3api create-bucket --bucket "$BUCKET_NAME" --region "$REGION"
            else
              aws s3api create-bucket \
                --bucket "$BUCKET_NAME" \
                --region "$REGION" \
                --create-bucket-configuration LocationConstraint="$REGION"
            fi
            
            echo "🔐 Enabling versioning..."
            aws s3api put-bucket-versioning \
              --bucket "$BUCKET_NAME" \
              --versioning-configuration Status=Enabled
            
            echo "🔒 Enabling encryption..."
            aws s3api put-bucket-encryption \
              --bucket "$BUCKET_NAME" \
              --server-side-encryption-configuration '{
                "Rules": [{
                  "ApplyServerSideEncryptionByDefault": {
                    "SSEAlgorithm": "AES256"
                  }
                }]
              }'
            
            echo "🚫 Blocking public access..."
            aws s3api put-public-access-block \
              --bucket "$BUCKET_NAME" \
              --public-access-block-configuration \
                BlockPublicAcls=true,IgnorePublicAcls=true,BlockPublicPolicy=true,RestrictPublicBuckets=true
            
            echo "✅ Bucket $BUCKET_NAME created and configured successfully!"
          else
            echo "✅ Bucket $BUCKET_NAME already exists"
          fi

      - name: Set up Terraform
        uses: hashicorp/setup-terraform@v3

      - name: Create Terraform Backend Configuration
        run: |
          cd infra
          cat > backend.tf <<EOF
          terraform {
            backend "s3" {
              bucket  = "${{ needs.setup.outputs.s3_bucket }}"
              key     = "${{ needs.setup.outputs.s3_key }}"
              region  = "${{ needs.setup.outputs.backend_region }}"
              encrypt = true
            }
          }
          EOF
          echo "✅ backend.tf created successfully"
          cat backend.tf

      - name: Terraform Init
        run: |
          cd infra
          terraform init

      - name: Terraform Plan
        run: |
          cd infra
          terraform plan -input=false -var="aws_region=${{ needs.setup.outputs.aws_region }}"

      - name: Terraform Apply
        run: |
          cd infra
          if [ "${{ needs.setup.outputs.tf_auto_approve }}" = "true" ]; then
            terraform apply -auto-approve -input=false -var="aws_region=${{ needs.setup.outputs.aws_region }}"
          else
            terraform apply -input=false -var="aws_region=${{ needs.setup.outputs.aws_region }}"
          fi

  terraform_destroy:
    needs: setup
    if: ${{ needs.setup.outputs.tf_destroy == 'true' }}
    runs-on: ubuntu-latest
    environment: prod
    steps:
      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-region: ${{ needs.setup.outputs.aws_region }}
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}

      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ needs.setup.outputs.python_version }}

      - name: Set up Terraform
        uses: hashicorp/setup-terraform@v3

      - name: Create Terraform Backend Configuration
        run: |
          cd infra
          cat > backend.tf <<EOF
          terraform {
            backend "s3" {
              bucket  = "${{ needs.setup.outputs.s3_bucket }}"
              key     = "${{ needs.setup.outputs.s3_key }}"
              region  = "${{ needs.setup.outputs.backend_region }}"
              encrypt = true
            }
          }
          EOF
          echo "✅ backend.tf created successfully"

      - name: Terraform Init
        run: |
          cd infra
          terraform init

      - name: Terraform Plan (Destroy)
        run: |
          cd infra
          terraform plan -destroy -input=false -var="aws_region=${{ needs.setup.outputs.aws_region }}"

      - name: Terraform Destroy
        run: |
          cd infra
          terraform destroy -auto-approve -input=false -var="aws_region=${{ needs.setup.outputs.aws_region }}"